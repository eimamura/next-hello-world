import React from "react";

export default ({ children }: { children: React.ReactNode }) => {
    return (
        <main>
            <h1>Logo - Layout - Menu</h1>
            {children}
        </main>
    )
}